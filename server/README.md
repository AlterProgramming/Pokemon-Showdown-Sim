# Server

This folder holds the runtime pieces that serve models back to the simulator.

- `EntityServerRuntime.py`: shared runtime helpers for entity-based serving
- `ModelServerRouting.py`: model-selection logic for API requests
- `serve_entity_model_benchmark.py`: single-model benchmark server entrypoint

