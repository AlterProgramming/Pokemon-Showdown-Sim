# Core ML stack
# Requires Python 3.12 (project standard).
# Note: tensorflow==2.20.0 / keras==3.13.2 used in production (Windows/Linux).
# On macOS Intel the latest available versions are pinned below.
tensorflow==2.16.2
keras==3.10.0
numpy

# Kaggle dataset download (public datasets — no credentials required)
kagglehub

# Model server (used by server/ and EntityServerRuntime)
Flask==3.1.3

# Testing
pytest

# Structured logging (audit-approved 2026-04-20; pure-Python, no CVEs, 4-5M weekly DLs)
structlog>=24.0,<25
